<table style="width:100%">
  <tr>
    <th>Save Time</th>
    <th>Title</th> 
    <th>URL</th>
  </tr>
  <% @articles.each do |a| %>
    <tr>
      <td><%= a.created_at %></td>
      <td><%= a.title %></td>
      <td><a href="<%= a.url %>"><%= a.url %></a></td>
    </tr>
  <% end %>
</table>

